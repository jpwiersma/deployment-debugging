# Laravel Deploy Test

Diagnostic Laravel application for verifying Strackt's deployment pipeline — detection, provisioning, and runtime behavior. Each branch is a config variant; the diagnostic page at `/` reports what was declared vs. what's actually running.

See `CLAUDE.md` for the branch strategy and diagnostic architecture.
